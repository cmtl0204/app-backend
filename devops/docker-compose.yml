services:
  app-backend:
    build:
      context: ..
      dockerfile: devops/Dockerfile

    container_name: app-backend

    restart: unless-stopped

    env_file:
      - ../.env

    networks:
      - npm_network

networks:
  npm_network:
    external: true